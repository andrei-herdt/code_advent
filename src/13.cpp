#include <assert.h>
#include <fstream>
#include <iostream>
#include <regex>
#include <vector>

using namespace std;

int main() {
    /* Generate map of the road (x,y)->tracktype
     * Get states vector (x,y,direction) of all cars
     * sort according x, y
     * Update position and direction according to order
     * Check state vector for equal positions
     */

    // serial_number = 71;
    // assert(power_level(101, 153) == 4);
}
