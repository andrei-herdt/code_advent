#include <fstream>
#include <iostream>
#include <regex>
#include <vector>

using namespace std;

int main() {
    /*Generate map
     * Compute are sizes for 3x3 squares
     * Pick the biggest
     */

    // std::cout << "scores: " << std::endl;
    // for (auto i : scores) {
    //     std::cout << i << " ";
    // }
    // std::cout << std::endl;
}
